{
  "name": "kareer-client",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "prepare": "lefthook install"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.3.0",
    "@commitlint/config-conventional": "^20.3.0",
    "@kareer/prettier-config": "workspace:*",
    "lefthook": "^2.0.13",
    "prettier": "^3.7.4",
    "turbo": "^2.7.2",
    "typescript": "5.9.2"
  },
  "prettier": "@kareer/prettier-config",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=18"
  }
}
