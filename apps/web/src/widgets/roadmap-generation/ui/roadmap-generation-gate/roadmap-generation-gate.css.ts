import { style } from '@vanilla-extract/css';

export const loadingCardContainer = style({
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center',
  width: '100%',
  height: '100%',
});
