import { style } from '@vanilla-extract/css';

export const actionListContainer = style({
  marginTop: '1.6rem',
});

export const lottieIcon = style({
  marginTop: '1.2rem',
  display: 'flex',
  justifyContent: 'center',
});
